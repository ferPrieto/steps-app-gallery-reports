format_version: 1
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

app:
  envs: 
  - APP_ID: ${APP_ID}
  - CLIENT_ID: ${CLIENT_ID}
  - CLIENT_SECRET: ${CLIENT_SECRET}
  - DOWNLOAD_INSTALLATION_LANGUAGE: ${DOWNLOAD_INSTALLATION_LANGUAGE}
  - DOWNLOAD_INSTALLATION_START_TIME: ${DOWNLOAD_INSTALLATION_START_TIME}
  - DOWNLOAD_INSTALLATION_END_TIME: ${DOWNLOAD_INSTALLATION_END_TIME}
  - DOWNLOAD_INSTALLATION_EXPORT_TYPE: ${DOWNLOAD_INSTALLATION_EXPORT_TYPE} 
  - DOWNLOAD_INSTALLATION_REPORT_LINK: ${DOWNLOAD_INSTALLATION_REPORT_LINK} 
  - IN_APP_PAYMENT_LANGUAGE: ${IN_APP_PAYMENT_LANGUAGE}
  - IN_APP_PAYMENT_START_TIME: ${IN_APP_PAYMENT_START_TIME}
  - IN_APP_PAYMENT_END_TIME: ${IN_APP_PAYMENT_END_TIME}
  - IN_APP_PAYMENT_EXPORT_TYPE: ${IN_APP_PAYMENT_EXPORT_TYPE} 
  - IN_APP_PAYMENT_REPORT_LINK: ${IN_APP_PAYMENT_REPORT_LINK} 
  - IN_APP_PAYMENT_REPORT_CURRENCY: ${IN_APP_PAYMENT_REPORT_CURRENCY} 
  - PAID_DOWNLOAD_DOMAIN: ${PAID_DOWNLOAD_DOMAIN}
  - PAID_DOWNLOAD_LANGUAGE: ${PAID_DOWNLOAD_LANGUAGE}
  - PAID_DOWNLOAD_START_TIME: ${PAID_DOWNLOAD_START_TIME}
  - PAID_DOWNLOAD_END_TIME: ${PAID_DOWNLOAD_END_TIME}
  - PAID_DOWNLOAD_COUNTRY_ID: ${PAID_DOWNLOAD_COUNTRY_ID} 
  - PAID_DOWNLOAD_EXPORT_TYPE: ${PAID_DOWNLOAD_EXPORT_TYPE} 
  - INSTALLATION_FAILURE_DATA_LANGUAGE: ${INSTALLATION_FAILURE_DATA_LANGUAGE}
  - INSTALLATION_FAILURE_DATA_START_TIME: ${INSTALLATION_FAILURE_DATA_START_TIME}
  - INSTALLATION_FAILURE_DATA_END_TIME: ${INSTALLATION_FAILURE_DATA_END_TIME} 

  # If you want to share this step into a StepLib
  - BITRISE_STEP_ID: steps-app-gallery-reports
  - BITRISE_STEP_VERSION: "0.9.0"
  - BITRISE_STEP_GIT_CLONE_URL: https://github.com/ferPrieto/steps-app-gallery-reports
  - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL

workflows:
  test:
    steps:
    - change-workdir:
        title: Switch working dir to test / _tmp dir
        description: |-
          To prevent step testing issues, like referencing relative
          files with just './some-file' in the step's code, which would
          work for testing the step from this directory directly
          but would break if the step is included in another `bitrise.yml`.
        run_if: true
        inputs:
        - path: ./_tmp
        - is_create_path: true
    - path::./:
        title: Step Test
        description: |-
          The example input has a default value,
          you can overwrite it if you want to, just like we did below,
          but the step would use the default value specified in the `step.yml`
          file if you would not specify another value.
        run_if: true
        inputs:
        - huawei_app_id: ${APP_ID}
        - huawei_client_id: ${CLIENT_ID}
        - huawei_client_secret: ${CLIENT_SECRET}
        - download_installation_language: ${DOWNLOAD_INSTALLATION_LANGUAGE}
        - download_installation_start_time: ${DOWNLOAD_INSTALLATION_START_TIME}
        - download_installation_end_time: ${DOWNLOAD_INSTALLATION_END_TIME}
        - download_installation_export_type: ${DOWNLOAD_INSTALLATION_EXPORT_TYPE}
        - in_app_payment_language: ${IN_APP_PAYMENT_LANGUAGE}
        - in_app_payment_start_time: ${IN_APP_PAYMENT_START_TIME}
        - in_app_payment_end_time: ${IN_APP_PAYMENT_END_TIME}
        - in_app_payment_export_type: ${IN_APP_PAYMENT_EXPORT_TYPE}
        - in_app_payment_currency: ${IN_APP_PAYMENT_CURRENCY}
        - paid_download_domain: ${PAID_DOWNLOAD_DOMAIN}
        - paid_download_language: ${PAID_DOWNLOAD_LANGUAGE}
        - paid_download_start_time: ${PAID_DOWNLOAD_START_TIME}
        - paid_download_end_time: ${PAID_DOWNLOAD_END_TIME}
        - paid_download_country_id: ${PAID_DOWNLOAD_COUNTRY_ID} 
        - paid_download_export_type: ${PAID_DOWNLOAD_EXPORT_TYPE}
        - installation_failure_data_language: ${INSTALLATION_FAILURE_DATA_LANGUAGE}
        - installation_failure_data_start_time: ${INSTALLATION_FAILURE_DATA_START_TIME}
        - installation_failure_data_end_time: ${INSTALLATION_FAILURE_DATA_END_TIME} 
        - show_debug_logs: ${SHOW_DEBUG_LOGS}
        outputs:
        - download_installation_report_link: ${DOWNLOAD_INSTALLATION_REPORT_LINK}
  # ----------------------------------------------------------------
  # --- workflows to Share this step into a Step Library
  audit-this-step:
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            stepman audit --step-yml ./step.yml

  share-this-step:
    envs:
      # if you want to share this step into a StepLib
      - MY_STEPLIB_REPO_FORK_GIT_URL: $MY_STEPLIB_REPO_FORK_GIT_URL
      - BITRISE_STEP_ID: $BITRISE_STEP_ID
      - BITRISE_STEP_VERSION: $BITRISE_STEP_VERSION
      - BITRISE_STEP_GIT_CLONE_URL: $BITRISE_STEP_GIT_CLONE_URL
    description: |-
      If this is the first time you try to share a Step you should
      first call: $ bitrise share

      This will print you a guide, and information about how Step sharing
      works. Please read it at least once!

      As noted in the Step sharing guide you'll have to fork the
      StepLib you want to share this step into. Once you're done with forking
      the repository you should set your own fork's git clone URL
      in the `.bitrise.secrets.yml` file, or here in the `envs` section,
      as the value of the `MY_STEPLIB_REPO_FORK_GIT_URL` environment.

      You're now ready to share this Step, just make sure that
      the `BITRISE_STEP_ID` and `BITRISE_STEP_VERSION`
      environments are set to the desired values!

      To share this Step into a StepLib you can just run: $ bitrise run share-this-step

      Once it finishes the only thing left is to actually create a Pull Request,
      the way described in the guide printed at the end of the process.
    before_run:
    - audit-this-step
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            bitrise share start -c "${MY_STEPLIB_REPO_FORK_GIT_URL}"
            bitrise share create --stepid "${BITRISE_STEP_ID}" --tag "${BITRISE_STEP_VERSION}" --git "${BITRISE_STEP_GIT_CLONE_URL}"
            bitrise share finish
